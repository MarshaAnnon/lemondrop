<div class="form">
    <%= form_with model: @book.quotes.build, local: true do | f |  %>
        <%= f.hidden_field :book_id, value: @book.id %>
        <%= f.label :quote %>:<br><br>
        <%= f.text_area :content, size: "60x6" %><br><br>
        <%= f.submit %>
    <% end %>
</div>